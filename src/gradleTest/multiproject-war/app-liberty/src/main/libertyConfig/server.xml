<?xml version="1.0" encoding="UTF-8"?>
<server description="Test server">

    <include optional="true" location="mps_server.xml"/>

    <!-- Enable features -->
    <featureManager>
        <feature>webProfile-6.0</feature>
        <feature>collectiveMember-1.0</feature>
        <feature>ssl-1.0</feature>
    </featureManager>


    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="${default.http.port}"
                  httpsPort="${default.https.port}" />

    <!-- clientAuthenticationSupported set to enable bidirectional trust -->
    <ssl id="defaultSSLConfig"
         sslProtocol="TLSv1.2"
         keyStoreRef="defaultKeyStore"
         trustStoreRef="defaultTrustStore"
         clientAuthenticationSupported="true" />

 
    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>

    <application context-root="app-war" type="war" id="local_app-war" location="${WORKING_DIR}/dropins/app-war.war" name="local_app-war">
       <classloader commonLibraryRef="SHARED_LIBRARY" />
    </application>

</server>

